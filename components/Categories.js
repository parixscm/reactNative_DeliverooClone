import { ScrollView, Text, View } from "react-native";

const Categories = () => {
  return (
    <ScrollView>
      {/* Category Cards */}
      <Text>Categories Hello</Text>
    </ScrollView>
  );
};
export default Categories;
